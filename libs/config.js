// Destructure the environment variables
export const {
  MONGODB_URL,
  VAPID_PRIVATE_KEY,
  NEXT_PUBLIC_VAPID_PUBLIC_KEY,
  AUTH_SECRET,
  __NEXT_PRIVATE_ORIGIN,
  NEXT_PUBLIC_API_URL,
} = process.env;
